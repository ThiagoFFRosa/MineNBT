import { expect,it } from 'vitest';import { resourceParts,safeAssetPath } from './paths';it('blocks traversal in asset paths',()=>{expect(()=>safeAssetPath('../secret')).toThrow('Unsafe');expect(()=>resourceParts('minecraft:../../secret','models')).toThrow('Unsafe');expect(resourceParts('minecraft:block/furnace','models').path).toBe('assets/minecraft/models/block/furnace.json')});
