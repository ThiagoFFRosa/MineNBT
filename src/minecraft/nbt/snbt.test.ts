import {describe,expect,it} from 'vitest';import {importItemSnbt,parseSnbt,stringifySnbt} from './snbt';import {createItem,setContainerSlot,treeMetrics} from '../item/model';
describe('SNBT',()=>{it('parses modern item components and escaped strings',()=>{const item=importItemSnbt(`{id:minecraft:stick,count:2,components:{"minecraft:custom_name":{text:"A \\"name\\"",italic:0b}}}`);expect(item.id).toBe('minecraft:stick');expect((item.components['minecraft:custom_name'] as any).text).toBe('A "name"')});it('round trips nested ItemStacks semantically',()=>{const root=setContainerSlot(createItem('minecraft:chest'),4,setContainerSlot(createItem('minecraft:shulker_box'),0,createItem('minecraft:diamond')));const parsed=importItemSnbt(stringifySnbt(root));expect(treeMetrics(parsed)).toMatchObject({totalItems:3,maxDepth:2});expect((parsed.components['minecraft:container'] as any)[0].slot).toBe(4)});it('parses numeric suffixes used by fixtures',()=>{expect(parseSnbt('{byte:1b,double:1.5d,long:2L}')).toEqual({byte:1,double:1.5,long:2})})});
