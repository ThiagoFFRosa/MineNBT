import { describe,expect,it } from 'vitest';import { ModelResolver,resolveTexture } from './modelResolver';
describe('model resolver',()=>{it('merges parents, textures, elements and gui display',async()=>{const data:Record<string,unknown>={'assets/minecraft/models/item/child.json':{parent:'minecraft:block/base',textures:{side:'#all'}},'assets/minecraft/models/block/base.json':{textures:{all:'minecraft:block/stone'},elements:[{from:[0,0,0],to:[16,16,16],faces:{north:{texture:'#side'}}}],display:{gui:{rotation:[30,225,0]}}}};const result=await new ModelResolver(async p=>data[p]).resolve('minecraft:item/child');expect(result.chain).toEqual(['minecraft:block/base','minecraft:item/child']);expect(result.elements).toHaveLength(1);expect(result.display?.gui?.rotation).toEqual([30,225,0]);expect(resolveTexture('#side',result.textures)).toContain('stone.png')});it('rejects parent cycles',async()=>{const r=new ModelResolver(async p=>p.includes('/a.json')?{parent:'minecraft:item/b'}:{parent:'minecraft:item/a'});await expect(r.resolve('minecraft:item/a')).rejects.toThrow('cycle')});it('rejects texture reference cycles',()=>expect(()=>resolveTexture('#a',{a:'#b',b:'#a'})).toThrow('cycle'))});
